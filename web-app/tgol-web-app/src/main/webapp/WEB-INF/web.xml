<?xml version="1.0" encoding="UTF-8"?>
<web-app version="2.4" 
    xmlns="http://java.sun.com/xml/ns/j2ee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://java.sun.com/xml/ns/j2ee
        http://java.sun.com/xml/ns/j2ee/web-app_2_4.xsd">

    <!-- context param configuration that locates spring security configuration
    file and the main spring context configuration file -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>
            /WEB-INF/conf/tgol-service.xml
            /WEB-INF/conf/tgol-security.xml
        </param-value>
    </context-param>

    <!-- listener classes -->
    <listener>
        <listener-class>
            org.springframework.web.context.ContextLoaderListener
        </listener-class>
    </listener>
    <!-- unnecessary, this functionality is already handled by DispatcherServlet
    <listener>
        <listener-class>
            org.springframework.web.context.request.RequestContextListener
        </listener-class>
    </listener>
     -->
     <!-- unnecessary??? Never seen a reference to this
    <listener>
        <!-This one is needed to enable the session management of spring security->
        <listener-class>
            org.springframework.security.web.session.HttpSessionEventPublisher
        </listener-class>
    </listener>
    -->
    <!-- This listener is set to access properties from the jsp-->
    <listener>
        <listener-class>
            org.opens.tgol.util.webapp.ConfigPropertiesExposerListener
        </listener-class>
    </listener>

    <!-- servlet dispatcher declaration -->
    <servlet>
        <servlet-name>tgol-web-app</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    
    <!-- servlet mapping declaration -->
    <servlet-mapping>
        <servlet-name>tgol-web-app</servlet-name>
        <url-pattern>*.html</url-pattern>
    </servlet-mapping>

    <servlet-mapping>
        <servlet-name>tgol-web-app</servlet-name>
        <url-pattern>/dispatch.html</url-pattern>
    </servlet-mapping>

    <!-- encoding filter declaration -->
    <filter>
        <filter-name>encoding-filter</filter-name>
        <filter-class>
            org.springframework.web.filter.CharacterEncodingFilter
        </filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <!-- filter mapping that applies the encoding filter to all urls -->
    <filter-mapping>
        <filter-name>encoding-filter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

<!--  Used to do some server-side pagination for site audits -->
    <filter>
        <filter-name>ResponseOverrideFilter</filter-name>
        <filter-class>org.displaytag.filter.ResponseOverrideFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>ResponseOverrideFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

<!--  binds a JPA EntityManager to the thread for the entire processing of the request. param is default value -->
    <filter>
        <filter-name>openEntityManagerInViewFilter1</filter-name>
        <filter-class>
            org.springframework.orm.jpa.support.OpenEntityManagerInViewFilter
        </filter-class>
        <init-param>
            <param-name>entityManagerFactoryBeanName</param-name>
            <param-value>entityManagerFactory</param-value>
        </init-param>
    </filter>

<!--  Using Spring Security filter to provide URL-based access control, applies on all pages -->
    <filter>
        <filter-name>springSecurityFilterChain</filter-name>
        <filter-class>
            org.springframework.web.filter.DelegatingFilterProxy
        </filter-class>
    </filter>

    <filter-mapping>
        <filter-name>springSecurityFilterChain</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    

    <welcome-file-list>
        <welcome-file>dispatch.html</welcome-file>
    </welcome-file-list>

    <!-- session timeout configuration : set to 60 min as default -->
    <session-config>
        <session-timeout>60</session-timeout>
    </session-config>
    
    <error-page>
        <error-code>404</error-code>
        <location>/access-denied.html</location>
    </error-page>
    <error-page>
        <error-code>405</error-code>
        <location>/access-denied.html</location>
    </error-page>
</web-app>